/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 public/cat.glb 
Author: Nyilonelycompany (https://sketchfab.com/Nyilonelycompany)
License: CC-BY-NC-SA-4.0 (http://creativecommons.org/licenses/by-nc-sa/4.0/)
Source: https://sketchfab.com/3d-models/saber-tooth-cat-c91a524d7a69442fb436b4a04d6d8878
Title: Saber Tooth Cat
*/

import React from 'react'
import { useGraph } from '@react-three/fiber'
import { useGLTF, useAnimations } from '@react-three/drei'
import { SkeletonUtils } from 'three-stdlib'

export function Model(props) {
  const { animationState = 'walk', ...restProps } = props
  const group = React.useRef()
  const { scene, animations } = useGLTF('/cat.glb')
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone)
  const { actions } = useAnimations(animations, group)

  // Play animation based on state
  React.useEffect(() => {
    if (actions) {
      // Stop all animations first
      Object.values(actions).forEach(action => action.stop())

      // Play the appropriate animation
      let animationName = 'Walk' // Default animation name
      if (animationState === 'death') animationName = 'Die'
      else if (animationState === 'run') animationName = 'Run'
      else if (animationState === 'walk') animationName = 'Walk'

      if (actions[animationName]) {
        actions[animationName].play()
        if (animationState === 'death') {
          actions[animationName].setLoop(false, 1) // Play death animation once
        }
      }
    }
  }, [actions, animationState])
  return (
    <group ref={group} {...restProps} dispose={null}>
      <group name="Sketchfab_Scene">
        <group name="Sketchfab_model" rotation={[-Math.PI / 2, 0, 0]} scale={1.19}>
          <group name="root">
            <group name="GLTF_SceneRootNode" rotation={[Math.PI / 2, 0, 0]}>
              <group name="Tiger_Rigao_46" scale={0.93}>
                <group name="GLTF_created_0">
                  <primitive object={nodes.GLTF_created_0_rootJoint} />
                  <group name="saber_toothed_tiger001_45" />
                  <skinnedMesh name="Object_7" geometry={nodes.Object_7.geometry} material={materials.Mat_sabar_teeth_Cat} skeleton={nodes.Object_7.skeleton} castShadow />
                </group>
              </group>
            </group>
          </group>
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/cat.glb')
